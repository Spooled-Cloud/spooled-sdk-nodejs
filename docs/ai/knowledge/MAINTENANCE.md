# Maintenance

| Change | Update |
|--------|--------|
| Version / UA | `package.json`, lock root, `src/config.ts` `SDK_VERSION` + `userAgent`, changelog |
| gRPC defaults / loader | `src/grpc/`, `bugs/findings.md`, contracts |
| Worker progress / job APIs | dataflows + findings if parity changes |
| Auth headers | contracts |

Keep same-artifact version surfaces synchronized before publish.
