# Spooled Node SDK — Agent Knowledge Base

Package: `@spooled/sdk` **1.0.39** (`SDK_VERSION` / User-Agent `@spooled/sdk-nodejs/1.0.39` in `src/config.ts`).

## Read order

1. This file → `MAINTENANCE.md` → `dataflows/` → `contracts/` → `bugs/findings.md` → `risk/`
2. Workspace contracts (if present): `docs/ai/workspace/CROSS-REPO-CONTRACTS.md`

## Surface

REST resources via `SpooledClient` + worker + realtime + full gRPC (unary + streams). Auth REST Bearer; gRPC `x-api-key`.
